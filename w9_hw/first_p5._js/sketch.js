function setup() {
  createCanvas(640, 480);
}

function draw() {
  background('#333333');
  noStroke();
  fill('cornflowerblue');
  // for (column = 0; column < width; column += 40) {
  //   for (let row = 0; row < height; row += 40) {
  //     let x = 20 + column;
  //     let y = height * 0.5;
  //     let diameter = 30;
  //     circle(x, y, diameter);
  //   }
  // } //모든 변수 let으로 시작.
  for(let row = 0; row < tileNumY; row++){
    for(column = 0; column < tileNumX; column++){
     let tileW = width/column;
     let tileH = height/row;
     let 
     let 
    }
  }
}
